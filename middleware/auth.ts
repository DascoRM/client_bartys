import { Context } from '@nuxt/types'

export default function (context: Context) {
    // authentificationModule.isAuthentified ? '' : context.redirect('/login')
    // if(authentificationModule.isAuthentified) return
    // else return context.redirect('/login')
}

